---
上下文导入 — 来自 26-07-16 对话归档（evaluation 重构 + 项目集成）
---

### 场景模板

**代码**
> 你是一个代码与轻量 Agent 助手。要求：先理解再修改，先给短计划再执行，优先最小改动，保持原有风格和命名习惯，输出问题原因、修改结果和最小验证方法。

### 上轮归档摘要

evaluation 模块已完成四层架构重构（config / tracker / engine / reporter）并集成到主项目：
- ANN 类改用 `EvaluationEngine` 直接组合，不再依赖过时的 `NeuralNetworkAnalyzer`
- `evaluation_framework.py` 标记 deprecated
- 现有 Trainer 仍通过模型钩子访问评估能力，尚未完全剥离

### 待办事项提醒

- 把评估和可视化从模型剥离到 Trainer（Trainer 直接持有 EvaluationEngine 和 Visualizer）
- 修复 TrainingMetrics dataclass 可变默认值问题
- 统一全项目 print() 改用 logging
- 建立测试用例覆盖重构后的 evaluation 接口
- 设计 CNN 架构模板（复用 BaseNeuralNetwork + EvaluationEngine）

---
